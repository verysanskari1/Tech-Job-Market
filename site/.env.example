NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: logo.dev publishable token for high-res brand logos.
# Sign up at https://logo.dev (free tier), copy the pk_... key here.
# When unset, the site falls back to Google's favicon proxy.
NEXT_PUBLIC_LOGO_DEV_TOKEN=
