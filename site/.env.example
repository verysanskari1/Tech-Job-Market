NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# logo.dev publishable token for high-res brand logos.
# Public key — safe to commit/expose. If unset, code falls back to the
# default baked into CompanyLogo. Override here when rotating.
NEXT_PUBLIC_LOGO_DEV_TOKEN=pk_NNwvFnj4TEqB0N1c9E10kA
