{
  "name": "scraper-qa",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "check": "npm run build && node dist/check.js",
    "check:verbose": "npm run build && VERBOSE=1 node dist/check.js"
  },
  "dependencies": {
    "@supabase/postgrest-js": "^1.15.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.11.0"
  }
}
